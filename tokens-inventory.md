# Token Template Inventory

Available template IDs you can reference in scripts (e.g., `CREATE template=<id> ...`):

- `token-small`
- `token-medium`
- `token-large`

These are defined in `tokens.js` and carry the default SVG cap, base size, and tint for each template. Add new templates there if you need more IDs.***
